<li>
  <%= gravatar_for user, size: Settings.avata_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("static_page.user.delete"), user, method: :delete, data: { confirm: t("static_page.user.confirm") } %>
  <% end %>
</li>
